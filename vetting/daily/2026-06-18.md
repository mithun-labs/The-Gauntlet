# Vetting Pass — 2026-06-18

## Executive Summary

**FIRST RUN — and a BLOCKED run.** The `vetting/` archive was initialized this run
(empty index/queue + directory tree). No EAs could be vetted because the core
evidence-gathering capability is unavailable in this environment.

- **Blocker (critical):** `WebFetch` returns **HTTP 403 Forbidden on every URL tested**,
  including the three target firm sites, MQL5, secondary blogs, and even
  `https://example.com/` and `https://en.wikipedia.org/`. The failure is environment-wide
  egress blocking, not site-specific bot protection. `WebSearch` works and returns
  result snippets / AI summaries, but per **HARD RULES 1 & 2** those are *leads only, not
  sources* — a claim may enter the archive only after the underlying page is actually
  fetched and its URL recorded. With no working fetch tool, there is no valid unit of
  evidence to record.
- **Rulebooks refreshed:** NONE. The RULEBOOK FRESHNESS CHECK could not run. On a first
  run there is no stored rulebook to fall back to (the "site unreachable → keep stale
  rulebook" recovery path requires a previously-stored rulebook, which does not exist
  yet), so **zero** firm rulebooks could be established from a primary source. Vetting any
  EA is therefore impossible — every legality verdict requires a fresh primary rulebook.
- **Outstanding rulebook re-check backlog:** N/A (no rulebooks exist).
- **Sources consulted (fetched pages only):** NONE. All fetch attempts returned 403 (see
  "Fetch attempts" below). WebSearch was used only to confirm the blocker and to probe
  whether the failure was site-specific (it is not).
- **EAs vetted: 0** (surviving gates: 0 · excluded at Gate A: 0).
- **Outcome contradictions surfaced:** none (empty outcomes ledger; first run).
- **Best evidence found this run:** none — no page could be fetched, so no evidence of any
  tier exists.
- **Major unverifiable claims:** N/A — no EA was profiled.
- **Why no Deployable verdict:** no EA was vetted; the run could not begin substantive work.
- **Best of pass:** nothing — no EA reached even an Avoid verdict.

## Decision: do not fabricate an archive from snippets

WebSearch returned plausible-looking summaries of FundedNext / Funding Pips / The Funded
Trader rules and of "best prop-firm EA" listicles. Writing rulebooks or EA files from that
material would violate HARD RULES 1 ("never fabricate") and 2 ("fetched public pages are
the unit of evidence; snippets and AI summaries are leads only, not sources"). A correct
empty result is the intended behavior of this agent under these conditions. No rulebook
files, EA files, or excluded files were created from unfetched content.

## Fetch attempts (all failed — HTTP 403)

| URL | Purpose | Result |
|-----|---------|--------|
| https://help.fundednext.com/en/articles/8020763-is-ea-allowed-in-fundednext | FundedNext EA policy | 403 |
| https://fundednext.com/blog/prop-firm-trading-rules | FundedNext rulebook | 403 |
| https://fundingpips.com/ | Funding Pips rules entry | 403 |
| https://thefundedtraderprogram.com/ | The Funded Trader rules entry | 403 |
| https://tradingfinder.com/props/funding-pips/rules/ | secondary rules (control test) | 403 |
| https://www.mql5.com/en/market/mt5/expert | EA candidate discovery | 403 |
| https://en.wikipedia.org/wiki/Expert_Advisor | control test (neutral domain) | 403 |
| https://example.com/ | control test (canonical reachable site) | 403 |

The two neutral control URLs (`example.com`, Wikipedia) returning 403 confirm the block is
at the environment egress layer, not the firms' bot protection.

## EAs This Pass — grouped by VERDICT
[Deployable]  none
[Watchlist]   none
[Avoid]       none

## Excluded at Gate A This Pass
none

## Rulebook Changes & Bounded Re-Check
None. No rulebooks could be created or refreshed.

## Red Flags Detected
- **Infrastructure:** `WebFetch` is non-functional (environment-wide 403). The Gauntlet's
  methodology is built on fetching primary firm rulebooks and independent EA evidence;
  without it the agent cannot produce a defensible verdict and must not invent one.

## Deployable / Watchlist Shortlist Changes
None.

## Action Required (operator)
The remote environment's **network policy is blocking outbound WebFetch egress** (all URLs
return 403, including neutral control sites). To let this routine do real work, the
environment needs an outbound network policy that permits WebFetch to reach the public web
(at minimum the three firm domains, MQL5, Myfxbook/FXBlue, ForexFactory, Reddit,
Trustpilot). See the network-policy section of
https://code.claude.com/docs/en/claude-code-on-the-web . Until then, every run will
correctly produce an empty archive rather than fabricated verdicts.
